spring:
  datasource:
    url: jdbc:postgresql://localhost:54321/order_automation
    driver-class-name: org.postgresql.Driver
    username: root
    password: somePassword

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true